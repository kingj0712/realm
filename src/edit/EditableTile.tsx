// Deprecated as of round 8: layout chrome (drag handle / resize handle / delete
// button / overlay) moved into `Overview.tsx`, which now uses
// react-grid-layout. This file is kept as an empty module so any lingering
// imports don't break, but nothing here is rendered.
export {};
