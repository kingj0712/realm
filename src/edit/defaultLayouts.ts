// Re-exported here for any older code that still imports `defaultOverviewLayout`.
// All actual layout templates live in `./sampleLayouts.ts`.
export { welcomeLayout as defaultOverviewLayout } from './sampleLayouts';
